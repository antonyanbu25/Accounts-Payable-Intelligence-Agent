# Runtime dependencies for the DEPLOYED service only (main.py and everything
# it imports: compute.py, diff.py, narration_guard.py, models.py,
# tax_lookup.py). Verified by grep/AST that this import chain needs none of
# torch, sentence-transformers, anthropic, or psycopg2-binary -- those are
# one-time LOCAL TOOLING dependencies for ingest.py only (see
# requirements-ingest.txt), never loaded by the live-serving process.
#
# This split is what fixed a real Render free-tier (512Mi) out-of-memory
# crash: sentence-transformers/torch were previously installed and *loaded*
# on every deploy just to rebuild a search index that doesn't change between
# deploys. Now the deploy only reads the pre-built index committed at
# service/chroma_db/ (see that directory's README), so those heavy
# dependencies never need to be installed on Render at all.
fastapi
uvicorn[standard]
python-dotenv
chromadb
pydantic
